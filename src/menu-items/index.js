import client from './client';
import admin from './admin';
import user from './user';
// import $ from 'juqery'

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  
  items: [user, admin, client]
};

export default menuItems;
